<div class="results">
    <div class="section-title">Calculated Ration</div>

    <div class="result-row">
        <span class="result-label" id="resLabelA">Corn</span>
        <span class="result-value green" id="resPctA">—</span>
    </div>
    <div class="result-row">
        <span class="result-label" id="resLabelB">Soybean Meal</span>
        <span class="result-value rust" id="resPctB">—</span>
    </div>
    <div class="result-row">
        <span class="result-label">Parts A : Parts B</span>
        <span class="result-value amber" id="resParts">—</span>
    </div>
    <div class="result-row">
        <span class="result-label">Actual CP Achieved</span>
        <span class="result-value amber" id="resActual">—</span>
    </div>

    <div class="bar-wrap">
        <div class="bar-track">
            <div class="bar-a" id="barA">—</div>
            <div class="bar-b" id="barB">—</div>
        </div>
    </div>
</div>
<style>
    /* Results */
    .results {
        background: var(--brown-dark);
        border-radius: 4px;
        padding: 1.2rem;
        color: var(--cream);
    }

    .results .section-title {
        color: var(--amber);
        border-color: rgba(212, 131, 42, 0.3);
    }

    .result-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0.5rem 0;
        border-bottom: 1px solid rgba(245, 240, 232, 0.08);
        font-size: 0.88rem;
    }

    .result-row:last-child {
        border-bottom: none;
    }

    .result-label {
        color: rgba(245, 240, 232, 0.7);
        font-weight: 300;
    }

    .result-value {
        font-family: "Source Code Pro", monospace;
        font-weight: 600;
        font-size: 1rem;
    }

    .result-value.green {
        color: var(--green-light);
    }
    .result-value.rust {
        color: #e07050;
    }
    .result-value.amber {
        color: var(--amber);
    }

    /* Bar chart */
    .bar-wrap {
        margin-top: 0.8rem;
    }
    .bar-track {
        height: 28px;
        border-radius: 3px;
        overflow: hidden;
        display: flex;
        background: rgba(245, 240, 232, 0.1);
    }

    .bar-a,
    .bar-b {
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-family: "Source Code Pro", monospace;
        font-size: 0.72rem;
        font-weight: 600;
        transition: width 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
        overflow: hidden;
        white-space: nowrap;
    }

    .bar-a {
        background: var(--green-field);
        color: white;
    }
    .bar-b {
        background: var(--rust);
        color: white;
    }

    .error-msg {
        background: rgba(184, 76, 26, 0.2);
        border: 1px solid var(--rust);
        border-radius: 3px;
        padding: 0.6rem 0.8rem;
        font-size: 0.8rem;
        color: #e07050;
        margin-top: 0.5rem;
        display: none;
    }
</style>
